<Page
    x:Class="SpartanHub.Views.AuthPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid
            Grid.Row="0"
            Padding="16,12"
            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="4">
                <TextBlock
                    FontSize="20"
                    FontWeight="SemiBold"
                    Text="HaloWaypoint 登录" />
                <TextBlock
                    x:Name="AddressText"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    Text="https://www.halowaypoint.com/sign-in?path=/halo-infinite/progression"
                    TextTrimming="CharacterEllipsis" />
            </StackPanel>

            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    x:Name="BackButton"
                    Click="BackButton_Click"
                    Content="后退" />
                <Button
                    x:Name="RefreshButton"
                    Click="RefreshButton_Click"
                    Content="刷新" />
                <Button
                    x:Name="CompleteLoginButton"
                    Click="CompleteLoginButton_Click"
                    Content="完成登录" />
            </StackPanel>
        </Grid>

        <WebView
            x:Name="LoginWebView"
            Grid.Row="1"
            NavigationCompleted="LoginWebView_NavigationCompleted"
            NavigationStarting="LoginWebView_NavigationStarting" />

        <Grid
            Grid.Row="2"
            Padding="16,10"
            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ProgressRing
                x:Name="LoadingRing"
                Width="24"
                Height="24"
                Margin="0,0,12,0"
                IsActive="False" />

            <TextBlock
                x:Name="StatusText"
                Grid.Column="1"
                VerticalAlignment="Center"
                Text="请在页面中完成 Microsoft / HaloWaypoint 登录，然后点击完成登录。"
                TextWrapping="Wrap" />

            <Button
                x:Name="ClearLoginButton"
                Grid.Column="2"
                Click="ClearLoginButton_Click"
                Content="清除登录" />
        </Grid>
    </Grid>
</Page>
